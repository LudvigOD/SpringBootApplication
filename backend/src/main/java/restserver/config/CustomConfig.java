package restserver.config;

/*
 * I thought we could use this class to store some custom configuration, like
 * constants and stuff. I don't know if we'll need it, but I thought it might
 * be useful.
 */
public class CustomConfig {
  public static final String TIME_FORMAT = "HH:mm:ss";
}
